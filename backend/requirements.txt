# MOAG Backend — nur GUI-relevante Pakete
# (extrahiert aus OCRexpert requirements.txt, ohne OCR/ML-Abhaengigkeiten)

# Web-Framework
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
gunicorn>=22

# HTTP-Client (fuer Adapter-HTTP-Calls)
httpx>=0.27

# Daten-Validierung
pydantic>=2.6

# Dev / Test
pytest>=8.0
pytest-asyncio>=0.23
pytest-httpx>=0.30
